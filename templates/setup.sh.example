#!/usr/bin/env bash
# .agent/setup.sh — prepare a FRESH worktree (worktrees start without deps).
# Without this, the gate fails on build for the wrong reason.
set -euo pipefail
(cd app && npm ci)
# (cd svc && go mod download)
